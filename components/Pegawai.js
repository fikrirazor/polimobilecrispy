import React, { Component } from 'react';
import { Button, View, Text} from 'react-native';

class Pegawai{
  contructor(no_peg,nama_peg, username, password){
    this.no_peg = no_peg;
    this.nama_peg = nama_peg;
    this.username = username;
    this.password = password;
  }
  get usr() {
    return
  }
}
